# 文档修订记录

日期：2026-09-20。本轮仅修改说明并增加CPU测试依赖清单，不修改模型、控制代码或运行配置；文档经审阅后提交至代码仓库。

## 修订内容

- 新增决赛材料入口、依赖与运行指南、配置说明、权重说明。
- 加入ModelScope地址并明确模型文件完整性尚未核验。
- 将旧快照中的“无模型内引导”限定为历史描述，指出当前源码已含引导分支。
- 修正“SOURCE_SHA256.md已提供上游revision”的不实描述。
- 对右腕映射冲突保留两份记录，未擅自选择现场值。
- 补充同步模式配置档说明，区分同步15步前缀和RTC第5步触发。
- 保留历史哈希，列出当前文件与历史记录的不一致；不重新标注为已现场验证。
- 说明录像工具路径硬编码及外部依赖，避免给出虚构命令参数。

## 尚需现场补交的事实材料

| 项目 | 当前缺口 |
|---|---|
| 最终代码版本 | 现场两端加载文件与提交号的对应关系 |
| 上游版本 | XPolicyLab/OpenPI准确提交号及未提交改动 |
| 依赖 | 两端实际Python版本、软件包锁定记录，模型端驱动/JAX环境 |
| 模型端配置 | 实际部署YAML、完整启动命令、train_config_name及repo_id |
| 权重 | ModelScope文件清单、revision、校验值、是否需基础权重 |
| 关节映射 | 现场生效值及验证记录 |
| 模型内RTC结果 | 与同一版本绑定的延迟/连续性测试和真机结果 |

本文档不以待补字段替代已验证结果。未具备上述证据前，只能称为“部署代码与历史记录整理版”，不能声明已完成最终现场一致性认证。
