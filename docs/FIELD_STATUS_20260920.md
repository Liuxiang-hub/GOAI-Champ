# 2026-09-20 现场运行核查

根据当日约18:21起的只读现场检查整理，时间均为Asia/Shanghai。本记录总结已有
配置与日志；原始视频、日志和访问凭据不随仓库发布。本轮未重新派发真机任务。

## 当前执行方案

测评主机运行Ubuntu 22.04.4，策略目录为`/home/user/.xrobot/XPolicyLab/policy/HRT/`。
该目录的`deploy.py`每次episode开始时读取同目录`motion_gate.json`，以其中的
`execution_mode`选择执行循环。

| 参数 | 现场读取值 | 公开发布配置 |
|---|---|---|
| execution_mode | synchronous_prefix | synchronous_prefix |
| control_hz | 25 | 25 |
| execute_steps | 15 | 15 |
| hardware_output_enabled | true | false |
| software_safety_enabled | false | false |

现场`deploy.yml`仍残留实验模式字段，执行循环以门控文件为准。本轮同步修订
仓库两份默认配置的模式字段，保留公开版本的硬件输出门控。仓库不是现场目录的
完整逐文件镜像，最终源码、检查点及依赖仍需绑定核验。

当前方案采用同步前缀控制。RTC及模型内引导保留为实验功能；团队反馈其真机
表现尚未达到预期，现有材料不足以量化收益或退化幅度。

## 已有真机记录

现场日志`/home/user/.xrobot/logs/log_20260920_153205.txt`记录了三路Orbbec图像流、
双臂环境初始化以及任务episode保存。核查时该日志更新至18:06:54。

| 任务 | 最近核查到的episode | HDF5保存时间 | 视频保存时间 |
|---|---|---|---|
| stack_bowls | 27 | 17:28:43 | 17:28:58 |
| stand_up_bottles | 6 | 18:03:18 | 18:03:33 |
| fill_pen_holder | 30 | 18:06:36 | 18:06:54 |

产物位于测评机`/home/user/eval-runner-slim-0.1.1/data/<task>/x-one-piperX-orbbec/`。
这些记录说明已有真机环境运行及数据产物；日志未包含上述任务的人工成功判定。
episode编号不能直接作为成功次数或本次试验次数。

## 性能与版本证据范围

另一份历史结构化日志属于2026-09-19的`FINAL`策略、50步动作块配置，与当前HRT
的15步执行前缀不同，且缺少充分的端点绑定证据。本页不将其延迟数字作为当前
6007链路或当前现场方案的性能指标。

L20的6007适配服务与测评机本地策略服务须分别识别。核查时6007存在监听，
其适配日志未提供可用于本次性能统计的完整请求记录。采集日志、历史回放和当前
配置分别说明不同事实，不能合并为一项端到端成功率或延迟结论。

正式结果需绑定源码提交或哈希、模型revision、实际配置、端点、任务、试验次数
及人工结果；关节映射验证和现场依赖锁定记录仍需补充。
